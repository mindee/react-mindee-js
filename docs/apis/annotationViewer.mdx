---
name: AnnotationViewer
route: /annotation-viewer
menu: Component API
---

### AnnotationViewer

> **AnnotationViewer use [KonvaJs](https://konvajs.org) HTML5 2d canvas js library for desktop and mobile applications**

#### image

- Type : `string`
- Required : `true`

> Image URL or base64. If you have an object of type [File](https://developer.mozilla.org/fr/docs/Web/API/File) ( typically generated by file upload libraries) in hand you must use [URL.createObjectURL()](https://developer.mozilla.org/fr/docs/Web/API/URL/createObjectURL) to generate a valid URL object.

---

#### shapes

- Type : `Array`
- Default : `[]`

> List of shapes to draw on canvas. Check [ShapeConfig](https://konvajs.org/api/Konva.Line.html#main) to custom shape style and behaviour for each state.

```js
{
    id: string
    coordinates: number[][]
    crop?: string
    options?: {
        default?: ShapeConfig
        onMouseEnter?: ShapeConfig
        onMouseLeave?: ShapeConfig
  }
}
```

---

#### options

- Type : `Object`
- Default:

```javascript
  {
    backgroundColor: 'black',
    shape: {
        default: {
            stroke: 'red',
            strokeWidth: 3,
            listening: true,
        },
        onMouseEnter: {},
        onMouseLeave: {},
    },
    zoom: {
        modifier: 1.2,
        max: 10,
        defaultZoom: 1,
    },
  }

```

> - Object of options to change default style and behavior. Here you can pass a custom style to all shape as you may do for each one.
> - Note that **shape** object overrides shapes individual custom style.

```js
{
  backgroundColor?: string
  zoom?: {
    modifier: number
    max: number
    defaultZoom: number
  }
  shape?: {
    default?: ShapeConfig
    onMouseEnter?: ShapeConfig
    onMouseLeave?: ShapeConfig
  }
}
```

---

#### orientation

- Type : `Number`
- Default : `0`

> Rotation in degree applied to the provided image

---

#### on-shape-mouse-enter

- Type : `Function`

> Return the shape on mouse enter event. Check [ShapeConfig](https://konvajs.org/api/Konva.Line.html#main) to custom shape style and behaviour for each state.

```javascript
{
  id: string
  coordinates: number[][]
  crop?: string
  options?: {
    default?: ShapeConfig
    onMouseEnter?: ShapeConfig
    onMouseLeave?: ShapeConfig
  }
}
```

---

#### on-shape-mouse-leave

- Type : `Function`

> Return the shape on mouse leave event. Check [ShapeConfig](https://konvajs.org/api/Konva.Line.html#main) to custom shape style and behaviour for each state.

```javascript
{
  id: string
  coordinates: number[][]
  crop?: string
  options?: {
    default?: ShapeConfig
    onMouseEnter?: ShapeConfig
    onMouseLeave?: ShapeConfig
  }
}
```

---

#### on-shape-click

- Type : `Function`

> Return the shape on click event. Check [ShapeConfig](https://konvajs.org/api/Konva.Line.html#main) to custom shape style and behaviour for each state.

```javascript
{
  id: string
  coordinates: number[][]
  crop?: string
  options?: {
    default?: ShapeConfig
    onMouseEnter?: ShapeConfig
    onMouseLeave?: ShapeConfig
  }
}
```

---

#### on-pointer-move

- Type : `Function`

> Return the pointer position on mouse move

```javascript
{
    x:number,
    y: number
}
```

---

#### styles

- Type : `CSSProperties`

> Regular CSS object to style the container

---

#### className

- Type : `string`

> Container className
