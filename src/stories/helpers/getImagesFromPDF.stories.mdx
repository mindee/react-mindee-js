import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'
import LinkTo from '@storybook/addon-links/react'

<Meta title="Helpers/getImagesFromPDF" />

# getImagesFromPDF

> getImagesFromPDF is a async function that takes a pdf file as an argument and return all

```javascript
getImagesFromPDF(pdfFile).then((images) => {
  // Do something with images pdf pages
})
```

<LinkTo kind="Examples/PDF" style={{ color: '#1474f3' }}>
  See example
</LinkTo>
